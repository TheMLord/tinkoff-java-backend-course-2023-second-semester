app:
  scheduler:
    enable: true
    interval: 10s
    force-check-delay: 10s

  client-base-url:
    github-url: http://localhost:8080/repos/
    stackoverflow-url: http://localhost:8080/2.3/questions/

spring:
  application:
    name: scrapper

server:
  port: 8080

logging:
  config: classpath:log4j2-plain.xml
